#include<bits/stdc++.h>
using namespace std;
const int N = 1e9 + 7;
int32_t main()
{
   ios_base::sync_with_stdio(0);
    cin.tie(0);
    vector<long long>v;
    for(int i = 0; i < N; i++)
    {
      v[i] = i v[1] << '\n';


   }
}
