#include<bits/stdc++.h>
using namespace std;
int32_t main()
{
   ios_base::sync_with_stdio(0);
    cin.tie(0);
  int t;
  cin >> t;
  while(t--)
  {
  	int n , m;
  	cin >> n >> m;
    char c[m][m];
    while(n--)
    {
      char c[m][m];
      for(int i = 0; i < m; i++)
      {
      	for(int j = 0; j < m; j++)
      	{
      		cin >> c[i][j];
      	}
      }	
    }


  }
}